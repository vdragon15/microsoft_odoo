<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="action_create_installed_checksums" model="ir.actions.server">
            <field name="name">Create Installed Checksums</field>
            <field name="type">ir.actions.server</field>
            <field name="state">code</field>
            <field name="model_id" ref="base.model_ir_config_parameter"/>
            <field name="binding_model_id" ref="base.model_ir_config_parameter"/>
            <field name="code">records.create_installed_checksums()</field>
        </record>

</odoo>